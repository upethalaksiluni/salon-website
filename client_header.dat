<?php
// Get unread notifications count
include_once "notification_handler.php";
$notificationHandler = new NotificationHandler($conn);
$unreadNotifications = $notificationHandler->getUnreadCount('client', $_SESSION['user_id'] ?? null);
?>

<header class="client-header">
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#clientNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>

            <a href="client_dashboard.php" class="logo-container">
                <img src="assets/images/logo.png" alt="Sulochana Salon" class="logo-img">
                <div class="brand-wrapper">
                    <h1 class="brand-text">Sulochana Salon</h1>
                    <p class="brand-subtext">Beauty & Wellness</p>
                </div>
            </a>
            
            <div class="collapse navbar-collapse" id="clientNavbar">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item me-3">
                        <a href="client_notifications.php" class="nav-link notifications">
                            <i class="fas fa-bell"></i>
                            <?php if ($unreadNotifications > 0): ?>
                                <span class="notification-badge"><?php echo $unreadNotifications; ?></span>
                            <?php endif; ?>
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle user-dropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <?php if (!empty($userProfile['profile_image'])): ?>
                                <img src="<?php echo htmlspecialchars($userProfile['profile_image']); ?>" alt="Profile" class="profile-image">
                            <?php else: ?>
                                <i class="fas fa-user-circle"></i>
                            <?php endif; ?>
                            <span><?php echo htmlspecialchars($_SESSION['username'] ?? 'User'); ?></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <a class="dropdown-item" href="view_profile.php">
                                    <i class="fas fa-user"></i> My Profile
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="edit_profile.php">
                                    <i class="fas fa-user-edit"></i> Edit Profile
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="change_password.php">
                                    <i class="fas fa-key"></i> Change Password
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="logout.php">
                                    <i class="fas fa-sign-out-alt"></i> Logout
                                </a>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>

<style>
@import 'variables.css';

.notification-icon {
    position: relative;
    margin-right: 15px;
}

/* Update notification styles */
.notifications {
    position: relative;
    margin-right: 1rem;
}

.notification-badge {
    position: absolute;
    top: -8px;
    right: -8px;
    background: var(--danger-color);
    color: var(--text-light);
    border-radius: 50%;
    min-width: 20px;
    height: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
    font-weight: bold;
    border: 2px solid var(--text-light);
    box-shadow: var(--box-shadow);
}

.notifications .btn {
    position: relative;
    padding: 0.5rem 0.75rem;
    font-size: 18px;
    border-radius: var(--border-radius);
    background: rgba(255, 255, 255, 0.1);
    color: var(--text-light);
    display: flex;
    align-items: center;
    gap: 5px;
    transition: var(--transition);
}

.notifications .btn i {
    font-size: 20px;
}

.notifications .btn .notification-count {
    font-size: 12px;
    font-weight: 600;
    color: #666;
    margin-left: 2px;
}

.notifications .btn:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: var(--hover-transform);
}

.client-header {
    height: 80px;
    background: var(--secondary-gradient);
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1100;
    box-shadow: var(--box-shadow);
    transition: var(--transition);
    margin-bottom: 0; 
    padding-bottom: 0;
}

.navbar {
    height: 100%;
    padding: 0.75rem 1rem 1.25rem;
}

.navbar-brand {
    color: var(--text-light) !important;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 1rem;
}

.brand-text {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--text-light);
    letter-spacing: 0.5px;
    margin: 0;
    white-space: nowrap;
}

.brand-subtext {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
    letter-spacing: 1px;
    text-transform: uppercase;
}

/* User Dropdown */
.nav-link {
    color: var(--text-light) !important;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: var(--transition);
}

.nav-link:hover {
    transform: var(--hover-transform);
}

.dropdown-menu {
    border: none;
    border-radius: var(--border-radius);
    box-shadow: var(--box-shadow);
    margin-top: 0.5rem;
}

.dropdown-item {
    padding: 0.75rem 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: var(--transition);
}

.dropdown-item i {
    width: 20px;
    color: var(--primary-color);
}

.dropdown-item:hover {
    background: #f8f9fa;
    color: var(--primary-color);
    transform: var(--hover-transform);
}

/* Logo and Brand Styles */
.logo-container {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.375rem 0.75rem;
    background: rgba(255, 255, 255, 0.1);
    border-radius: var(--border-radius);
    transition: var(--transition);
    height: 45px;
}

.logo-container:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: var(--hover-transform);
}

.logo-img {
    height: 40px;
    width: auto;
    filter: brightness(0) invert(1);
    transition: var(--transition);
    object-fit: contain;
}

.brand-wrapper {
    display: flex;
    flex-direction: column;
    justify-content: center;
}

.brand-text {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--text-light);
    letter-spacing: 0.5px;
    margin: 0;
    line-height: 1.2;
}

.brand-subtext {
    font-size: 0.7rem;
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
    letter-spacing: 0.8px;
    text-transform: uppercase;
    line-height: 1;
}

/* Responsive Design */
@media (max-width: 768px) {
    .navbar {
        padding: 0.5rem 0.5rem 1rem;
    }

    .brand-text {
        font-size: 1rem;
    }

    .notifications .btn {
        padding: 0.4rem 0.6rem;
    }

    .logo-container {
        height: 40px;
        padding: 0.25rem 0.5rem;
    }

    .client-header {
        margin-bottom: 15px; 
    }

    .logo-img {
        height: 28px;
    }
    
    .brand-subtext {
        font-size: 0.6rem;
    }
}

/* Add these at the end of your client-header.css file */

@keyframes logoSpin {
    from {
        transform: rotateY(0deg);
    }
    to {
        transform: rotateY(360deg);
    }
}

.logo-container:hover .logo-img {
    animation: logoSpin 1s ease-in-out;
}

@keyframes textGlow {
    0% {
        text-shadow: 0 0 5px rgba(255,255,255,0.3);
    }
    50% {
        text-shadow: 0 0 15px rgba(255,255,255,0.5);
    }
    100% {
        text-shadow: 0 0 5px rgba(255,255,255,0.3);
    }
}

.logo-container:hover .brand-text {
    animation: textGlow 1.5s ease-in-out infinite;
}
</style>